# Comments and blank lines are allowed.

RCS
.+,v

CVS
\.\#.+       # CVS conflict files / emacs lock files
\.cvsignore

\.svn
_darcs
\.hg

\.git
\.gitignore
\.gitmodules

.+~          # emacs backup files
\#.*\#       # emacs autosave files

^/README.*
^/LICENSE.*
^/COPYING

# setup files
scripts
things_to_install.txt
index.sh
CLAUDE.md
# NOTE: no bare `.claude` entry here. It is unanchored, so it would match the
# `claude` package's own `.claude/` mapping dir and stop it linking into
# ~/.claude. A stray root-level Claude *project* dir isn't a package, so stow
# never processes it anyway.

# log files 
.*\.log$
